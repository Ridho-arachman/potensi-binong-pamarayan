export const metadata = {
  title: "Kelola Potensi | Admin Desa Binong",
  description: "Kelola data potensi desa melalui panel admin.",
};

export default function KelolaPotensiLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
