export const metadata = {
  title: "Edit Potensi | Admin Desa Binong",
  description: "Edit data potensi desa melalui panel admin.",
};

export default function EditPotensiLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
